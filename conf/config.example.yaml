# RAGFlow Admin Configuration
# Copy to config.yaml and modify as needed

server:
  host: "0.0.0.0"
  port: 8080
  debug: true
  secret_key: "change-me-to-random-string"  # Change in production!

admin:
  username: "admin"
  password: "admin"

# MySQL and RAGFlow API can be configured via Settings page
mysql:
  host: ""
  port: 5455
  database: "rag_flow"
  user: ""
  password: ""

ragflow:
  base_url: ""
  api_key: ""
